<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/home"
        android:icon="@drawable/home"
        android:title="Главная"
        app:iconTint="@color/home_icon_color"
    android:titleTextColor="@color/home_text_color"
        />

    <item
        android:id="@+id/search"
        android:icon="@drawable/magnifyingwhite"
        android:title="Поиск"
        app:iconTint="@color/nav_icon_color"
    android:titleTextColor="@color/nav_text_color" />

    <item
        android:id="@+id/cart"
        android:icon="@drawable/tool"
        android:title="Корзина"
        app:iconTint="@color/nav_icon_color"
    android:titleTextColor="@color/nav_text_color" />

    <item
        android:id="@+id/profile"
        android:icon="@drawable/profile"
        android:title="Профиль"
        app:iconTint="@color/nav_icon_color"
    android:titleTextColor="@color/nav_text_color" />

</menu>